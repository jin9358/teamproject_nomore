<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.korit.nomoreback.domain.moim.MoimMapper">


    <insert id="createMoim" useGeneratedKeys="true" keyProperty="moimId">
        insert into moim_tb
        values (default,#{title},#{discription},#{memberCount},#{maxMember},#{date},#{time},#{moimImgPath},#{districtId},#{categoryId},#{userId})
    </insert>

</mapper>
